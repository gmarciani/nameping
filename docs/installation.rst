Installation
============

Install Nameping using pip:

.. code-block:: bash

   pip install nameping

Or install from source:

.. code-block:: bash

   git clone https://github.com/gmarciani/nameping.git
   cd nameping
   pip install -e .

Verify the installation:

.. code-block:: bash

   nameping --version
